# Редизайн лендинга нотариуса: «солидный нотариат»

Смена визуального языка сайта с SaaS-шаблона (синие градиенты, прыгающие анимации) на спокойный юридический стиль: тёмно-синий + золото/бежевый, serif-заголовки, мягкие тени вместо градиентов. Дополнительно — mobile-first проработка, hero с CTA-кнопками, блок доверия и FAQ-аккордеон.

Структура секций сохраняется: Header → Hero → Доверие → Услуги → FAQ → Расположение → Форма → Footer.

Параллельно — смена контактных данных: город меняется с Тараза на Караганду, вместе с адресом, телефоном, графиком работы и всей SEO-обвязкой.

**Актуальные данные (единый источник правды для всего плана):**
- Адрес: г. Караганда, проспект Бухар жырау, строение 49
- Телефон: `+7 705 737 2926` (`tel:+77057372926`)
- Часы работы: Пн-Сб 9:00-18:00, обед 13:00-14:00, воскресенье выходной

**Вне скоупа:** вынос токена Telegram-бота из клиентского кода (`src/ContactForm.jsx:16-17`) — делается отдельной задачей.

## Validation Commands
- `npm run build`
- `npx eslint src --ext .js,.jsx`

### Task 1: Смена контактных данных — адрес, телефон, график
- [x] Создать `src/config/contacts.js` с единым экспортом: адрес (город, улица, строение), телефон в форматах для отображения и для `tel:`/`wa.me`/`t.me`, часы работы с обедом и выходным
- [x] Заменить адрес в `src/Header.jsx:48-49`: «г.Тараз» / «ул.Толе Би, 53» → «г. Караганда» / «пр. Бухар жырау, стр. 49» (брать из конфига)
- [x] Заменить адрес в `src/Footer.jsx:65-66` на новый (брать из конфига)
- [x] Заменить график в `src/Footer.jsx:72-73`: сейчас «Пн-Пт: 9:00-18:00 / Сб: 9:00-14:00» → «Пн-Сб: 9:00-18:00», «Обед: 13:00-14:00», «Вс: выходной»
- [x] Заменить «Нотариальные услуги в Таразе» на «Нотариальные услуги в Караганде» в `src/Footer.jsx:92`
- [x] Проверить телефон во всех точках (`src/Header.jsx:25,29,33`, `src/Footer.jsx:39,46,49`) — номер `+77057372926` не меняется, но перевести все вхождения на конфиг
- [x] Уточнить у заказчика e-mail (требует подтверждения заказчика — не автоматизируемо). Временно сведён к `tusupov_71@mail.ru` из `public/index.html`, чтобы убрать привязанный к Таразу домен `notarius@taraz.kz`
- [x] Заменить embed-URL Google-карты в `src/App.js:59` — координаты Тараза убраны, iframe строится из `contacts.address.full` (пр. Бухар жырау, стр. 49, Караганда)
- [x] Проверить фото здания `src/img/054f8a21-...jpg` — требует нового фото от заказчика (не автоматизируемо), файл оставлен как есть

### Task 2: SEO-данные под новый город
- [x] Переписать `<meta name="keywords">` в `public/index.html:18`: заменить «нотариус тараз», «тараз» на карагандинские запросы
- [x] Обновить `<meta name="description">` в `public/index.html:19-31`: город, адрес, режим работы с обедом и выходным
- [x] Исправить «Нотариальная палата Жамбылской области» в `public/index.html:23` на Карагандинскую областную нотариальную палату. Использовано «Карагандинская областная нотариальная палата» — точное юридическое название требует подтверждения заказчика (не автоматизируемо)
- [x] Обновить `<title>` — добавить город: «Нотариус в Караганде — Тусупов Темиргали Расович»
- [x] Добавить JSON-LD schema `LegalService`/`Notary` с новым адресом, телефоном и `openingHoursSpecification` (Пн-Сб 9-18 с перерывом 13-14 — две смены 09:00-13:00 и 14:00-18:00, воскресенье не указано)
- [x] Добавить Open Graph теги (`og:title`, `og:description`, `og:image`, `og:locale=ru_RU`), плюс twitter-card
- [x] Обновить `name` в `package.json:2` с `taraz_notarius` на `karaganda_notarius`
- [x] Проверить `README.md` и `public/manifest.json` на упоминания старого города — упоминаний нет (дефолтный CRA-текст); `manifest.json` дополнительно назван по нотариусу, `theme_color` приведён к `#132a4a`

### Task 3: Дизайн-система — палитра, типографика, токены
- [x] Переписать `:root` в `src/index.css`: заменить синие `--primary-*` на тёмно-синий (`#132a4a`-класс), добавить `--gold`/`--accent-warm` (золото/бежевый), нейтрали `--bg-cream`, `--border-subtle`
- [x] Добавить шкалу отступов (`--space-1..--space-8`) и радиусов (`--radius-sm/md/lg`) вместо одного `--border-radius` (`--border-radius` сохранён как алиас `var(--radius-md)` — на него завязаны `App.css`/`Header.css`/`Footer.css`)
- [x] Смягчить тени: убрать `--shadow-xl` с большим blur, задать 3 уровня спокойных теней (`--shadow-xl` сведён к `var(--shadow-lg)` ради обратной совместимости)
- [x] Подключить serif-шрифт для заголовков (локально или `<link>` в `public/index.html`), задать `--font-heading` и `--font-body` — Playfair Display + Inter через Google Fonts с `preconnect`
- [x] Применить `--font-heading` к `h1..h6` в `src/index.css`, выставить `letter-spacing` и `line-height` под serif
- [x] Сократить `--transition` до `0.2s ease` и убрать глобальный `.btn:hover { transform: translateY(-2px) }`

### Task 4: Убрать визуальный шум и градиенты
- [x] Убрать `rotate()` из всех hover-эффектов: `src/Header.css` (`.icon-wrapper`, `.social-link.whatsapp`, `.social-link.telegram`), `src/App.css` (`.service-card:hover .fa-2x`), `src/Footer.css` (`.footer-social-link`)
- [x] Убрать `transform: translateX(3px)` с `.contact-link:hover` и `.footer-link:hover` — заменить на смену цвета/подчёркивание
- [x] Заменить градиентный текст на сплошной цвет: `.brand-name` в `src/Header.css` (убрать `-webkit-text-fill-color: transparent`), заголовок формы в `src/ContactForm.jsx`
- [x] Заменить `linear-gradient` фоны на сплошные/едва заметные тона: `.about-lawyer`, `.location-section` в `src/App.css`, `.modern-header` в `src/Header.css`. Дополнительно сняты градиенты с `.modern-footer`, соц-кнопок и submit-кнопки формы
- [x] Заменить градиентные подчёркивания `h3::after` в `src/App.css` на сплошную золотую линию (то же для `.footer-title::after`)
- [x] Заменить `border-image: linear-gradient(...)` в `.modern-header` на обычный `border-bottom`
- [x] Удалить неиспользуемый класс `.fade-in` и `@keyframes fadeIn` из `src/App.css` (либо применить его к секциям)
- [x] Уменьшить `service-card:hover` подъём с `translateY(-10px)` до `-4px`

### Task 5: Вынести инлайн-стили формы в CSS
- [x] Создать `src/ContactForm.css` со стилями карточки, инпутов, лейблов, кнопки и success-блока
- [x] Удалить все `style={{...}}` из `src/ContactForm.jsx`, заменить на классы
- [x] Удалить `onFocus`/`onBlur` хендлеры смены `borderColor` — заменить на CSS `:focus` / `:focus-visible`
- [x] Удалить `onMouseOver`/`onMouseOut` с кнопки submit — заменить на CSS `:hover`
- [x] Заменить инлайн-SVG success-иконки на компонент `SuccessIcon` с классом, стилизовать через CSS (`fill`/`stroke` через `currentColor`)
- [x] Применить новые токены дизайн-системы (цвета, радиусы, тени) вместо хардкод-значений

### Task 6: Hero-секция с CTA
- [x] Переписать блок `.about-lawyer` в `src/App.js`: заголовок, ФИО, короткий подзаголовок с городом (Караганда) и специализацией — вынесен в компонент `Hero` с `<section aria-labelledby>` и `<h1>`
- [x] Добавить строку CTA: кнопка «Позвонить» (`tel:+77057372926`), кнопка WhatsApp, вторичная кнопка «Записаться» со скроллом к форме
- [x] Добавить бейдж «Открыто сейчас / Обед до 14:00 / Закрыто» — вычислять по графику из `src/config/contacts.js` (Пн-Сб 9:00-18:00, перерыв 13:00-14:00, Вс выходной). Логика в `getWorkStatus(now)`, покрыта тестами на все ветки
- [x] Вывести часы работы под бейджем текстом, включая обед и выходной — из `contacts.schedule.lines`
- [x] Перенести текст лицензии из hero в компактный бейдж под CTA (`.hero-license`)
- [x] Стилизовать hero в `src/App.css` под новую палитру: тёмно-синий фон, золотой акцент на CTA, убрать SVG-паттерн `::before` или сделать его тоньше — паттерн удалён полностью, добавлена золотая нижняя граница
- [x] Добавить плавный скролл к форме (`scroll-behavior: smooth` + `id` на секции формы) — `html { scroll-behavior: smooth }` в `src/index.css`, якорь `#contact-form`

### Task 7: Блок доверия и преимуществ
- [x] Создать `src/TrustSection.jsx` и `src/TrustSection.css`
- [x] Добавить 4 плитки: лицензия №0003207 с 2010 года, опыт работы, конфиденциальность, оформление в день обращения
- [x] Стаж считать динамически от 2010 года (`new Date().getFullYear() - 2010`), не хардкодить число — иначе устареет через год. Год вынести константой в `src/config/contacts.js` — `PRACTICE_SINCE` + `getYearsOfPractice(now)`
- [x] Добавить склонение «лет/года/год» для числа стажа — `pluralizeYears()` / `formatYearsOfPractice()`, покрыто тестами на все ветки (1, 2-4, 5-20, 11-14, 21, 101, 112)
- [x] Использовать FontAwesome-иконки в единой стилистике (тонкие, золотой/тёмно-синий, без анимаций вращения)
- [x] Вставить `<TrustSection />` в `src/App.js` между hero и секцией услуг — порядок проверен тестом через `compareDocumentPosition`
- [x] Проверить сетку на 320px / 768px / 1200px — mobile-first: 1 колонка → 2 (`min-width: 768px`) → 4 (`min-width: 1200px`), проверено тестом на CSS

### Task 8: FAQ-аккордеон
- [x] Создать `src/Faq.jsx` и `src/Faq.css` с массивом из 6-8 вопросов (документы для сделки, стоимость, выезд на дом, сроки, наследство, доверенности) — 8 вопросов, включая отзыв доверенности и порядок приёма (график берётся из `contacts.schedule`)
- [x] Реализовать аккордеон на `useState` без Bootstrap JS (в проекте подключён только CSS Bootstrap) — одна открытая панель за раз, повторный клик сворачивает; покрыто тестом на отсутствие `data-bs-*`
- [x] Добавить доступность: `<button>` для триггера, `aria-expanded`, `aria-controls`, управление с клавиатуры — нативная `<button type="button">` даёт Enter/Space и таб-порядок из коробки, панель именована `aria-label` и скрывается через `hidden`
- [x] Стилизовать под новую палитру: тонкие разделители, золотой индикатор раскрытия — `--border-subtle` между вопросами, шеврон `--gold-dark` с поворотом на 180° при раскрытии, `:focus-visible` золотой рамкой
- [x] Вставить `<Faq />` в `src/App.js` между услугами и секцией расположения — порядок проверен тестом через `compareDocumentPosition`

### Task 9: Секция услуг и расположения
- [x] Выровнять карточки услуг в `src/App.css`: единая высота, спокойная рамка, hover без вращения иконки — `.card-body` стал flex-колонкой, `.card-text` тянется `flex-grow: 1`, bootstrap-класс `shadow` снят (тень только на hover)
- [x] Убрать фиксированные `width="500" height="670"` с iframe карты в `src/App.js`, задать адаптивный контейнер с `aspect-ratio` — обёртка `.location-map-frame` с `aspect-ratio: 4 / 3`, iframe на `position: absolute; inset: 0`
- [x] Выровнять фото и карту по высоте в `.location-content` — общая сетка `grid-template-columns: 1fr 1fr` с `align-items: stretch` от `min-width: 992px`, фото `object-fit: cover`; в одну колонку — `object-fit: contain`
- [x] Добавить `alt` с осмысленным описанием вместо `alt="Location"` в `src/App.js` — «Вход в нотариальную контору: {адрес из конфига}»
- [x] Добавить `loading="lazy"` и явные `width`/`height` фото для предотвращения CLS — `960×1280` (реальные размеры JPEG)
- [x] Добавить под картой ссылку «Построить маршрут» с координатами нового адреса в Караганде — `contacts.address.directionsUrl` (Google Maps Directions API по адресу, а не по координатам, чтобы не дублировать источник правды)
- [x] Продублировать адрес и часы работы текстом рядом с картой (не только внутри iframe — поисковики не читают его содержимое) — блок `<address class="location-details">` с адресом и `contacts.schedule.lines`

### Task 10: Mobile-first проработка
- [x] Переписать медиазапросы на mobile-first (`min-width` вместо `max-width`) в `src/App.css`, `src/Header.css`, `src/Footer.css` — базовые правила рассчитаны на 320px, расширения через `min-width: 768px/992px`; единственный `max-width` — верхняя граница диапазона «только планшет», уже открытого через `min-width`
- [x] Вернуть sticky-хедер на мобилке (`src/Header.css:159` отключает `position: sticky`) в компактном виде: имя + кнопка звонка — `.header-compact` + `.header-call` (44×44), развёрнутые контакты и адрес раскрываются от 768px
- [x] Добавить плавающую кнопку звонка/WhatsApp внизу экрана на мобилке (скрыта на десктопе) — `src/MobileCallBar.jsx` / `.css`, `position: fixed`, скрыт от `min-width: 992px`; под панель зарезервирован `padding-bottom` у `body` с `env(safe-area-inset-bottom)`
- [x] Довести все тач-таргеты до минимум 44×44px: `.social-link` в `src/Header.css` сейчас 28×28px — подняты `.social-link`, `.icon-wrapper`, `.header-call`, `.footer-link`, `.faq-trigger`, поля и кнопка формы; покрыто параметризованным тестом
- [x] Проверить hero, услуги, FAQ и форму на 320px — отсутствие горизонтального скролла: тест ловит любые фиксированные `width`/`min-width` больше 320px во всех CSS, плюс `overflow-x: hidden` у `body` как страховка
- [x] Заменить `type="text"` на `type="tel"` с `inputMode="tel"` для поля телефона в `src/ContactForm.jsx` — добавлен и `autoComplete="tel"`

### Task 11: Доступность и финальная проверка
- [x] Проверить контраст всех текстовых пар по WCAG AA, поправить светло-серый `--text-light` на тёмных/цветных фонах — 18 пар покрыты тестом с расчётом относительной яркости по токенам. Затемнены `--text-light` (`#5c6a7a` → `#556271`) и `--gold-dark` (`#8c6f38` → `#7d6231`, было 4.42:1 на кремовом). Фирменные цвета мессенджеров вынесены в токены `--whatsapp`/`--telegram` и затемнены: белый текст на `#128c7e` давал 4.14:1, на `#0088cc` — 3.89:1
- [x] Добавить видимый `:focus-visible` для всех интерактивных элементов (ссылки, кнопки, поля, FAQ-триггеры) — одно общее правило в `src/index.css` на `a/button/input/textarea/select/summary/[tabindex]` через токены `--focus-ring-*`; на тёмно-синих hero и подвале обводка переключается на `--gold-light`. Тест ловит компоненты, гасящие `outline` без своего индикатора
- [x] Заменить `<div>`-обёртки секций в `src/App.js` на `<section>` с `aria-labelledby` — услуги, расположение и форма стали регионами; заголовки секций подняты с `h3` до `h2` (единственный `h1` — ФИО в hero), селекторы в `App.css` обновлены
- [x] Исправить `<html lang="en">` на `lang="ru"` в `public/index.html` (сделано в Task 2)
- [x] Удалить дублирующий дефолтный CRA `<meta name="description">` в `public/index.html`, оставить один осмысленный (сделано в Task 2)
- [x] Добавить `@media (prefers-reduced-motion: reduce)` — отключение transform/анимаций: анимации и переходы сведены к 0.01ms, подъёмы карточек/соц-кнопок и поворот FAQ-шеврона отключены через `transform: none`, плавный скролл переведён в `auto`
- [x] Финальная проверка на остатки старых данных: `grep -rniE "тараз|taraz|толе|жамбыл" src public README.md package.json` — в исходниках и разметке чисто; совпадения остались только в самих тестах, которые проверяют отсутствие этих строк (`App.test.js`, `seo.test.js`, `accessibility.test.js`)
- [x] Проверить, что телефон, адрес и график везде берутся из `src/config/contacts.js`, а не продублированы строками — параметризованный тест по всем `.js/.jsx` в `src` ловит захардкоженные номер, улицу и часы работы
- [x] Запустить `npm run build`, убедиться в отсутствии ошибок и предупреждений — `CI=true npm run build` компилируется чисто (при `CI=true` предупреждения фатальны)
- [x] Прогнать `npx eslint src --ext .js,.jsx` и починить замечания — 0 ошибок; починен `jest/no-conditional-expect` в новом тесте